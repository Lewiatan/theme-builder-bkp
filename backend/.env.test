# Test environment overrides
APP_ENV=test
# Use DATABASE_URL from .env or environment variable (allows CI to override)
# For local Docker: postgresql://builder:builder@postgres:5432/builder_test
# For CI: postgresql://builder:builder@localhost:5432/builder_test
DATABASE_URL=${DATABASE_URL:-"postgresql://builder:builder@postgres:5432/builder_test?serverVersion=16&charset=utf8"}
DEFAULT_URI=http://localhost
CORS_ALLOW_ORIGIN=.*
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=
